import 'package:flutter/material.dart';

class LocationStoreView extends StatelessWidget {
  const LocationStoreView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
